#pragma once
#include <iostream>      // cout, endl
#include <vector>        // vector
#include <queue>         // priority_queue
#include <climits>       // INT_MAX
#include <iomanip>       // setw
#include <sstream>       // ostringstream
#include <algorithm>     // reverse, max
#include <fstream>       // ifstream, ofstream, fstream
#include <chrono>        // Đo thời gian thực thi
#include <functional>    // Dùng std::function và std::invoke để gọi hàm
#include <tuple>         // Sử dụng std::tuple để lưu cạnh (u, v, w)
#include <set>           // Dùng std::set để tránh cạnh trùng lặp
#include <random>        // Sinh số ngẫu nhiên (đỉnh, trọng số, v.v.)
